# Database
# Replace with your PostgreSQL connection string
DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@localhost:5432/cyberguard?schema=public"

# JWT Secret (IMPORTANT: Generate a strong random string for production)
# Use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"
JWT_EXPIRES_IN="7d"

# Encryption Key (REQUIRED: Generate a 64-character hex string for SMTP password encryption)
# Use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY="your-64-character-hex-encryption-key-change-this"

# Server
PORT=3000
NODE_ENV="development"

# Frontend URL (for CORS) - supports comma-separated values for multiple origins
# Example: "http://localhost:5173,http://localhost:5174,https://yourdomain.com"
FRONTEND_URL="http://localhost:5173"

# Google Gemini AI (Required for AI-powered features)
# Get your API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY="your-gemini-api-key-here"
