Run the backend test suite and report results.

Steps:
1. Run `cd backend && npm test` and capture output
2. If tests fail, show the failing test names, expected vs actual values, and file locations
3. If all pass, show a brief summary with count

Do NOT fix any failures automatically — just report them clearly.
