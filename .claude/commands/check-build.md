Run both frontend and backend builds and report any errors.

Steps:
1. Run `cd frontend && npm run build` and capture output
2. Run `cd backend && npm run build` and capture output
3. If either build fails, show the errors clearly with file paths and line numbers
4. If both succeed, confirm with a brief summary

Do NOT fix any errors automatically — just report them clearly so the user can decide what to do.
